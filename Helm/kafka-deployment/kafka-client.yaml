apiVersion: v1
kind: Pod
metadata:
  name: kafka-client
  namespace: nfe101
spec:
  containers:
  - name: kafka-client
    image: confluentinc/cp-kafka:latest
    command: ["/bin/sh"]
    args: ["-c", "sleep infinity"]
